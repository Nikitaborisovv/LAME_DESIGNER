# React 19 / R3F 9 / drei peer-зависимости иногда конфликтуют у транзитивных пакетов.
# Это снимает блокировку установки. Если хочешь строгую проверку — убери строку и чини вручную.
legacy-peer-deps=true
